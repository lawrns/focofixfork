import { describe, it, expect } from 'vitest'

describe('AI Decomposition (Legacy)', () => {
  it('placeholder: covered by provider integration tests with node runtime', () => {
    expect(true).toBe(true)
  })
})
