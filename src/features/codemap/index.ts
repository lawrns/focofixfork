/**
 * Codemap Integration Feature
 * Module 5: Codemap Integration
 * 
 * Parses project file structure, detects entry points, generates Mermaid
 * dependency graphs. Syncs to memory segments (Module 6) and mermaid service.
 */

export * from './types';
export * from './services/codemap-parser';
export * from './services/codemap-sync';
