import { describe, it, expect } from 'vitest'

describe('KanbanBoard Mobile Responsiveness (Legacy)', () => {
  it('placeholder: responsive behavior covered by active UI/e2e suites', () => {
    expect(true).toBe(true)
  })
})
