# ============================================================================
# FOCO - Environment Variables Template
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env or .env.local to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/foco

# ----------------------------------------------------------------------------
# Supabase Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Get these from your Supabase project dashboard: https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ----------------------------------------------------------------------------
# AI Configuration (Required for AI features)
# ----------------------------------------------------------------------------
# Primary AI Provider: glm (Z.AI), deepseek, or openai
# GLM (Z.AI) - https://z.ai/ - Recommended for best performance
AI_PROVIDER=glm
GLM_API_KEY=your-glm-api-key-here
GLM_MODEL=glm-4.7

# Alternative: OpenAI (also used for audio transcription)
# Get your API key from: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
NEXT_PUBLIC_OPENAI_MODEL=gpt-4o-mini

# Alternative: DeepSeek
# Get your API key from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your-deepseek-api-key-here
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# ----------------------------------------------------------------------------
# Redis Cache Configuration (REQUIRED for performance optimization)
# ----------------------------------------------------------------------------
# Sign up at https://upstash.com for a free Redis instance
# This enables 60-80% performance improvement through caching
UPSTASH_REDIS_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_TOKEN=your_upstash_redis_token

# ----------------------------------------------------------------------------
# Application Configuration
# ----------------------------------------------------------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ----------------------------------------------------------------------------
# WhatsApp Integration via Twilio (Optional)
# ----------------------------------------------------------------------------
# Set up Twilio WhatsApp at: https://console.twilio.com/
# Follow setup guide: https://www.twilio.com/docs/whatsapp/quickstart
#
# Steps to configure:
# 1. Create Twilio account at https://www.twilio.com/try-twilio
# 2. Get a WhatsApp-enabled phone number (or use sandbox for testing)
# 3. Create API Key at Console > Account > API keys & tokens
# 4. Configure webhook URL in Twilio Console > Messaging > Settings
# 5. (Optional) Connect your own WhatsApp Business number

# Twilio Account SID (starts with AC, from Console Dashboard)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Twilio API Key SID (starts with SK, from API Keys page)
TWILIO_API_KEY_SID=SKxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Twilio API Key Secret (shown once when creating API key)
TWILIO_API_KEY_SECRET=your_api_key_secret_here

# Your Twilio WhatsApp-enabled phone number (E.164 format)
TWILIO_WHATSAPP_NUMBER=+15557170836

# Redis Configuration (Required for WhatsApp session management)
# If using different Redis instance for WhatsApp, specify here:
# REDIS_HOST=your-redis-host.com
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# ----------------------------------------------------------------------------
# Security Notice
# ----------------------------------------------------------------------------
# IMPORTANT:
# - NEVER commit .env files to version control
# - Rotate all secrets immediately if exposed
# - Use different credentials for dev/staging/production
# - Store production secrets in secure vault (Vercel, AWS Secrets Manager, etc.)
# - Review .gitignore to ensure .env files are excluded
#
# Secret Rotation Schedule:
# - Database credentials: Every 90 days
# - API keys: Every 90 days
# - Supabase keys: Every 180 days or immediately if exposed
# - JWT secrets: Every 90 days
